query FetchCartDetails($cartId: String!) {
  cart(id: $cartId) {
    id
    key
    customerEmail
    totalLineItemQuantity
    locale
    totalPrice {
      currencyCode
      centAmount
      fractionDigits
    }
  }
}
